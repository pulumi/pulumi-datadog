// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.datadog;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Export;
import com.pulumi.core.annotations.ResourceType;
import com.pulumi.core.internal.Codegen;
import com.pulumi.datadog.TagRuleArgs;
import com.pulumi.datadog.Utilities;
import com.pulumi.datadog.inputs.TagRuleState;
import java.lang.Boolean;
import java.lang.Integer;
import java.lang.String;
import java.util.List;
import java.util.Optional;
import javax.annotation.Nullable;

/**
 * Provides a Datadog tag rule resource. This can be used to create and manage Datadog governance tag rules.
 * 
 * ## Example Usage
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.datadog.TagRule;
 * import com.pulumi.datadog.TagRuleArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         // Create a surfacing tag rule, which highlights non-compliant telemetry
 *         // without rejecting it.
 *         var env = new TagRule("env", TagRuleArgs.builder()
 *             .name("env must be a known environment")
 *             .source("logs")
 *             .scope("org")
 *             .tagKey("env")
 *             .tagValuePatterns(            
 *                 "prod",
 *                 "staging",
 *                 "dev")
 *             .ruleType("surfacing")
 *             .required(true)
 *             .build());
 * 
 *         // Create a blocking tag rule, which rejects telemetry that violates it.
 *         // The Datadog API only accepts `surfacing` at creation time, so the provider
 *         // creates the rule and then immediately updates it to `blocking`. Because that
 *         // makes the create non-atomic, it has to be opted into explicitly.
 *         var service = new TagRule("service", TagRuleArgs.builder()
 *             .name("service must be lowercase and hyphenated")
 *             .source("spans")
 *             .scope("org")
 *             .tagKey("service")
 *             .tagValuePatterns("web-*")
 *             .ruleType("blocking")
 *             .forceBlockingOnCreate(true)
 *             .hardDelete(false)
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ## Import
 * 
 * The `pulumi import` command can be used, for example:
 * 
 * ```sh
 * $ pulumi import datadog:index/tagRule:TagRule env &#34;&lt;rule_id&gt;&#34;
 * ```
 * 
 */
@ResourceType(type="datadog:index/tagRule:TagRule")
public class TagRule extends com.pulumi.resources.CustomResource {
    /**
     * The RFC 3339 timestamp at which the rule was created.
     * 
     */
    @Export(name="createdAt", refs={String.class}, tree="[0]")
    private Output<String> createdAt;

    /**
     * @return The RFC 3339 timestamp at which the rule was created.
     * 
     */
    public Output<String> createdAt() {
        return this.createdAt;
    }
    /**
     * The identifier of the user who created the rule.
     * 
     */
    @Export(name="createdBy", refs={String.class}, tree="[0]")
    private Output<String> createdBy;

    /**
     * @return The identifier of the user who created the rule.
     * 
     */
    public Output<String> createdBy() {
        return this.createdBy;
    }
    /**
     * Whether the rule is currently enforced. Defaults to `true`.
     * 
     */
    @Export(name="enabled", refs={Boolean.class}, tree="[0]")
    private Output<Boolean> enabled;

    /**
     * @return Whether the rule is currently enforced. Defaults to `true`.
     * 
     */
    public Output<Boolean> enabled() {
        return this.enabled;
    }
    /**
     * Set to `true` to allow creating a rule with `ruleType` set to `blocking`. The Datadog API only accepts `surfacing` at creation time, so the provider creates the rule as `surfacing` and then immediately updates it to `blocking`, which makes the create non-atomic: if the update fails, a `surfacing` rule is left behind and the resource is marked tainted. This field is only read during creation and is not sent to the API; changing it afterwards produces a diff that has no effect.
     * 
     */
    @Export(name="forceBlockingOnCreate", refs={Boolean.class}, tree="[0]")
    private Output</* @Nullable */ Boolean> forceBlockingOnCreate;

    /**
     * @return Set to `true` to allow creating a rule with `ruleType` set to `blocking`. The Datadog API only accepts `surfacing` at creation time, so the provider creates the rule as `surfacing` and then immediately updates it to `blocking`, which makes the create non-atomic: if the update fails, a `surfacing` rule is left behind and the resource is marked tainted. This field is only read during creation and is not sent to the API; changing it afterwards produces a diff that has no effect.
     * 
     */
    public Output<Optional<Boolean>> forceBlockingOnCreate() {
        return Codegen.optional(this.forceBlockingOnCreate);
    }
    /**
     * Whether destroying this resource permanently deletes the tag rule. When set to `false` the rule is soft-deleted instead, which keeps it recoverable and preserves its historical compliance score data. This field is only read during deletion and is not sent to the API on create or update. Defaults to `true`.
     * 
     */
    @Export(name="hardDelete", refs={Boolean.class}, tree="[0]")
    private Output<Boolean> hardDelete;

    /**
     * @return Whether destroying this resource permanently deletes the tag rule. When set to `false` the rule is soft-deleted instead, which keeps it recoverable and preserves its historical compliance score data. This field is only read during deletion and is not sent to the API on create or update. Defaults to `true`.
     * 
     */
    public Output<Boolean> hardDelete() {
        return this.hardDelete;
    }
    /**
     * The RFC 3339 timestamp at which the rule was last modified.
     * 
     */
    @Export(name="modifiedAt", refs={String.class}, tree="[0]")
    private Output<String> modifiedAt;

    /**
     * @return The RFC 3339 timestamp at which the rule was last modified.
     * 
     */
    public Output<String> modifiedAt() {
        return this.modifiedAt;
    }
    /**
     * The identifier of the user who last modified the rule.
     * 
     */
    @Export(name="modifiedBy", refs={String.class}, tree="[0]")
    private Output<String> modifiedBy;

    /**
     * @return The identifier of the user who last modified the rule.
     * 
     */
    public Output<String> modifiedBy() {
        return this.modifiedBy;
    }
    /**
     * Human-readable name for the tag rule.
     * 
     */
    @Export(name="name", refs={String.class}, tree="[0]")
    private Output<String> name;

    /**
     * @return Human-readable name for the tag rule.
     * 
     */
    public Output<String> name() {
        return this.name;
    }
    /**
     * When `true`, the rule matches tag values that do NOT match any of the supplied patterns. Defaults to `false`.
     * 
     */
    @Export(name="negated", refs={Boolean.class}, tree="[0]")
    private Output<Boolean> negated;

    /**
     * @return When `true`, the rule matches tag values that do NOT match any of the supplied patterns. Defaults to `false`.
     * 
     */
    public Output<Boolean> negated() {
        return this.negated;
    }
    /**
     * When `true`, telemetry without this tag is treated as a violation. Defaults to `false`.
     * 
     */
    @Export(name="required", refs={Boolean.class}, tree="[0]")
    private Output<Boolean> required;

    /**
     * @return When `true`, telemetry without this tag is treated as a violation. Defaults to `false`.
     * 
     */
    public Output<Boolean> required() {
        return this.required;
    }
    /**
     * How the rule is enforced. `surfacing` only highlights non-compliant telemetry, while `blocking` rejects telemetry that violates the rule. The API only accepts `surfacing` at creation time, so creating a rule directly as `blocking` requires `forceBlockingOnCreate` to be set to `true`. Using `blocking` at all requires blocking tag rules to be enabled for your organization; otherwise the API returns `403 permission denied`. Valid values are `blocking`, `surfacing`.
     * 
     */
    @Export(name="ruleType", refs={String.class}, tree="[0]")
    private Output<String> ruleType;

    /**
     * @return How the rule is enforced. `surfacing` only highlights non-compliant telemetry, while `blocking` rejects telemetry that violates the rule. The API only accepts `surfacing` at creation time, so creating a rule directly as `blocking` requires `forceBlockingOnCreate` to be set to `true`. Using `blocking` at all requires blocking tag rules to be enabled for your organization; otherwise the API returns `403 permission denied`. Valid values are `blocking`, `surfacing`.
     * 
     */
    public Output<String> ruleType() {
        return this.ruleType;
    }
    /**
     * The scope the rule applies within. Typically an environment, team, or organization-level identifier used to limit where the rule is enforced.
     * 
     */
    @Export(name="scope", refs={String.class}, tree="[0]")
    private Output<String> scope;

    /**
     * @return The scope the rule applies within. Typically an environment, team, or organization-level identifier used to limit where the rule is enforced.
     * 
     */
    public Output<String> scope() {
        return this.scope;
    }
    /**
     * The telemetry source that the tag rule applies to. This field cannot be updated after creation; changing it forces a new resource. Valid values are `logs`, `spans`, `metrics`, `rum`, `feed`.
     * 
     */
    @Export(name="source", refs={String.class}, tree="[0]")
    private Output<String> source;

    /**
     * @return The telemetry source that the tag rule applies to. This field cannot be updated after creation; changing it forces a new resource. Valid values are `logs`, `spans`, `metrics`, `rum`, `feed`.
     * 
     */
    public Output<String> source() {
        return this.source;
    }
    /**
     * The tag key that the rule governs (for example, `service`).
     * 
     */
    @Export(name="tagKey", refs={String.class}, tree="[0]")
    private Output<String> tagKey;

    /**
     * @return The tag key that the rule governs (for example, `service`).
     * 
     */
    public Output<String> tagKey() {
        return this.tagKey;
    }
    /**
     * One or more patterns that valid values for the tag key must match. At least one pattern is required. These are not regular expressions: the API restricts pattern characters to `A-Za-z0-9_:-.,/*`, with `*` acting as a wildcard. A pattern made up only of wildcards (for example a bare `*`) is rejected by the API.
     * 
     */
    @Export(name="tagValuePatterns", refs={List.class,String.class}, tree="[0,1]")
    private Output<List<String>> tagValuePatterns;

    /**
     * @return One or more patterns that valid values for the tag key must match. At least one pattern is required. These are not regular expressions: the API restricts pattern characters to `A-Za-z0-9_:-.,/*`, with `*` acting as a wildcard. A pattern made up only of wildcards (for example a bare `*`) is rejected by the API.
     * 
     */
    public Output<List<String>> tagValuePatterns() {
        return this.tagValuePatterns;
    }
    /**
     * A monotonically increasing version counter that is incremented on each update.
     * 
     */
    @Export(name="version", refs={Integer.class}, tree="[0]")
    private Output<Integer> version;

    /**
     * @return A monotonically increasing version counter that is incremented on each update.
     * 
     */
    public Output<Integer> version() {
        return this.version;
    }

    /**
     *
     * @param name The _unique_ name of the resulting resource.
     */
    public TagRule(java.lang.String name) {
        this(name, TagRuleArgs.Empty);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     */
    public TagRule(java.lang.String name, TagRuleArgs args) {
        this(name, args, null);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param options A bag of options that control this resource's behavior.
     */
    public TagRule(java.lang.String name, TagRuleArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("datadog:index/tagRule:TagRule", name, makeArgs(args, options), makeResourceOptions(options, Codegen.empty()), false);
    }

    private TagRule(java.lang.String name, Output<java.lang.String> id, @Nullable TagRuleState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("datadog:index/tagRule:TagRule", name, state, makeResourceOptions(options, id), false);
    }

    private static TagRuleArgs makeArgs(TagRuleArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        if (options != null && options.getUrn().isPresent()) {
            return null;
        }
        return args == null ? TagRuleArgs.Empty : args;
    }

    private static com.pulumi.resources.CustomResourceOptions makeResourceOptions(@Nullable com.pulumi.resources.CustomResourceOptions options, @Nullable Output<java.lang.String> id) {
        var defaultOptions = com.pulumi.resources.CustomResourceOptions.builder()
            .version(Utilities.getVersion())
            .build();
        return com.pulumi.resources.CustomResourceOptions.merge(defaultOptions, options, id);
    }

    /**
     * Get an existing Host resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state
     * @param options Optional settings to control the behavior of the CustomResource.
     */
    public static TagRule get(java.lang.String name, Output<java.lang.String> id, @Nullable TagRuleState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        return new TagRule(name, id, state, options);
    }
}
