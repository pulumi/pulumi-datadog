// *** WARNING: this file was generated by pulumi-language-dotnet. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

using System;
using System.Collections.Generic;
using System.Collections.Immutable;
using System.Threading.Tasks;
using Pulumi.Serialization;

namespace Pulumi.Datadog
{
    /// <summary>
    /// Provides a Datadog tag rule resource. This can be used to create and manage Datadog governance tag rules.
    /// 
    /// ## Example Usage
    /// 
    /// ```csharp
    /// using System.Collections.Generic;
    /// using System.Linq;
    /// using Pulumi;
    /// using Datadog = Pulumi.Datadog;
    /// 
    /// return await Deployment.RunAsync(() =&gt; 
    /// {
    ///     // Create a surfacing tag rule, which highlights non-compliant telemetry
    ///     // without rejecting it.
    ///     var env = new Datadog.TagRule("env", new()
    ///     {
    ///         Name = "env must be a known environment",
    ///         Source = "logs",
    ///         Scope = "org",
    ///         TagKey = "env",
    ///         TagValuePatterns = new[]
    ///         {
    ///             "prod",
    ///             "staging",
    ///             "dev",
    ///         },
    ///         RuleType = "surfacing",
    ///         Required = true,
    ///     });
    /// 
    ///     // Create a blocking tag rule, which rejects telemetry that violates it.
    ///     // The Datadog API only accepts `surfacing` at creation time, so the provider
    ///     // creates the rule and then immediately updates it to `blocking`. Because that
    ///     // makes the create non-atomic, it has to be opted into explicitly.
    ///     var service = new Datadog.TagRule("service", new()
    ///     {
    ///         Name = "service must be lowercase and hyphenated",
    ///         Source = "spans",
    ///         Scope = "org",
    ///         TagKey = "service",
    ///         TagValuePatterns = new[]
    ///         {
    ///             "web-*",
    ///         },
    ///         RuleType = "blocking",
    ///         ForceBlockingOnCreate = true,
    ///         HardDelete = false,
    ///     });
    /// 
    /// });
    /// ```
    /// 
    /// ## Import
    /// 
    /// The `pulumi import` command can be used, for example:
    /// 
    /// ```sh
    /// $ pulumi import datadog:index/tagRule:TagRule env "&lt;rule_id&gt;"
    /// ```
    /// </summary>
    [DatadogResourceType("datadog:index/tagRule:TagRule")]
    public partial class TagRule : global::Pulumi.CustomResource
    {
        /// <summary>
        /// The RFC 3339 timestamp at which the rule was created.
        /// </summary>
        [Output("createdAt")]
        public Output<string> CreatedAt { get; private set; } = null!;

        /// <summary>
        /// The identifier of the user who created the rule.
        /// </summary>
        [Output("createdBy")]
        public Output<string> CreatedBy { get; private set; } = null!;

        /// <summary>
        /// Whether the rule is currently enforced. Defaults to `True`.
        /// </summary>
        [Output("enabled")]
        public Output<bool> Enabled { get; private set; } = null!;

        /// <summary>
        /// Set to `True` to allow creating a rule with `RuleType` set to `Blocking`. The Datadog API only accepts `Surfacing` at creation time, so the provider creates the rule as `Surfacing` and then immediately updates it to `Blocking`, which makes the create non-atomic: if the update fails, a `Surfacing` rule is left behind and the resource is marked tainted. This field is only read during creation and is not sent to the API; changing it afterwards produces a diff that has no effect.
        /// </summary>
        [Output("forceBlockingOnCreate")]
        public Output<bool?> ForceBlockingOnCreate { get; private set; } = null!;

        /// <summary>
        /// Whether destroying this resource permanently deletes the tag rule. When set to `False` the rule is soft-deleted instead, which keeps it recoverable and preserves its historical compliance score data. This field is only read during deletion and is not sent to the API on create or update. Defaults to `True`.
        /// </summary>
        [Output("hardDelete")]
        public Output<bool> HardDelete { get; private set; } = null!;

        /// <summary>
        /// The RFC 3339 timestamp at which the rule was last modified.
        /// </summary>
        [Output("modifiedAt")]
        public Output<string> ModifiedAt { get; private set; } = null!;

        /// <summary>
        /// The identifier of the user who last modified the rule.
        /// </summary>
        [Output("modifiedBy")]
        public Output<string> ModifiedBy { get; private set; } = null!;

        /// <summary>
        /// Human-readable name for the tag rule.
        /// </summary>
        [Output("name")]
        public Output<string> Name { get; private set; } = null!;

        /// <summary>
        /// When `True`, the rule matches tag values that do NOT match any of the supplied patterns. Defaults to `False`.
        /// </summary>
        [Output("negated")]
        public Output<bool> Negated { get; private set; } = null!;

        /// <summary>
        /// When `True`, telemetry without this tag is treated as a violation. Defaults to `False`.
        /// </summary>
        [Output("required")]
        public Output<bool> Required { get; private set; } = null!;

        /// <summary>
        /// How the rule is enforced. `Surfacing` only highlights non-compliant telemetry, while `Blocking` rejects telemetry that violates the rule. The API only accepts `Surfacing` at creation time, so creating a rule directly as `Blocking` requires `ForceBlockingOnCreate` to be set to `True`. Using `Blocking` at all requires blocking tag rules to be enabled for your organization; otherwise the API returns `403 permission denied`. Valid values are `Blocking`, `Surfacing`.
        /// </summary>
        [Output("ruleType")]
        public Output<string> RuleType { get; private set; } = null!;

        /// <summary>
        /// The scope the rule applies within. Typically an environment, team, or organization-level identifier used to limit where the rule is enforced.
        /// </summary>
        [Output("scope")]
        public Output<string> Scope { get; private set; } = null!;

        /// <summary>
        /// The telemetry source that the tag rule applies to. This field cannot be updated after creation; changing it forces a new resource. Valid values are `Logs`, `Spans`, `Metrics`, `Rum`, `Feed`.
        /// </summary>
        [Output("source")]
        public Output<string> Source { get; private set; } = null!;

        /// <summary>
        /// The tag key that the rule governs (for example, `Service`).
        /// </summary>
        [Output("tagKey")]
        public Output<string> TagKey { get; private set; } = null!;

        /// <summary>
        /// One or more patterns that valid values for the tag key must match. At least one pattern is required. These are not regular expressions: the API restricts pattern characters to `A-Za-z0-9_:-.,/*`, with `*` acting as a wildcard. A pattern made up only of wildcards (for example a bare `*`) is rejected by the API.
        /// </summary>
        [Output("tagValuePatterns")]
        public Output<ImmutableArray<string>> TagValuePatterns { get; private set; } = null!;

        /// <summary>
        /// A monotonically increasing version counter that is incremented on each update.
        /// </summary>
        [Output("version")]
        public Output<int> Version { get; private set; } = null!;


        /// <summary>
        /// Create a TagRule resource with the given unique name, arguments, and options.
        /// </summary>
        ///
        /// <param name="name">The unique name of the resource</param>
        /// <param name="args">The arguments used to populate this resource's properties</param>
        /// <param name="options">A bag of options that control this resource's behavior</param>
        public TagRule(string name, TagRuleArgs args, CustomResourceOptions? options = null)
            : base("datadog:index/tagRule:TagRule", name, args ?? new TagRuleArgs(), MakeResourceOptions(options, ""))
        {
        }

        private TagRule(string name, Input<string> id, TagRuleState? state = null, CustomResourceOptions? options = null)
            : base("datadog:index/tagRule:TagRule", name, state, MakeResourceOptions(options, id))
        {
        }

        private static CustomResourceOptions MakeResourceOptions(CustomResourceOptions? options, Input<string>? id)
        {
            var defaultOptions = new CustomResourceOptions
            {
                Version = Utilities.Version,
            };
            var merged = CustomResourceOptions.Merge(defaultOptions, options);
            // Override the ID if one was specified for consistency with other language SDKs.
            merged.Id = id ?? merged.Id;
            return merged;
        }
        /// <summary>
        /// Get an existing TagRule resource's state with the given name, ID, and optional extra
        /// properties used to qualify the lookup.
        /// </summary>
        ///
        /// <param name="name">The unique name of the resulting resource.</param>
        /// <param name="id">The unique provider ID of the resource to lookup.</param>
        /// <param name="state">Any extra arguments used during the lookup.</param>
        /// <param name="options">A bag of options that control this resource's behavior</param>
        public static TagRule Get(string name, Input<string> id, TagRuleState? state = null, CustomResourceOptions? options = null)
        {
            return new TagRule(name, id, state, options);
        }
    }

    public sealed class TagRuleArgs : global::Pulumi.ResourceArgs
    {
        /// <summary>
        /// Whether the rule is currently enforced. Defaults to `True`.
        /// </summary>
        [Input("enabled")]
        public Input<bool>? Enabled { get; set; }

        /// <summary>
        /// Set to `True` to allow creating a rule with `RuleType` set to `Blocking`. The Datadog API only accepts `Surfacing` at creation time, so the provider creates the rule as `Surfacing` and then immediately updates it to `Blocking`, which makes the create non-atomic: if the update fails, a `Surfacing` rule is left behind and the resource is marked tainted. This field is only read during creation and is not sent to the API; changing it afterwards produces a diff that has no effect.
        /// </summary>
        [Input("forceBlockingOnCreate")]
        public Input<bool>? ForceBlockingOnCreate { get; set; }

        /// <summary>
        /// Whether destroying this resource permanently deletes the tag rule. When set to `False` the rule is soft-deleted instead, which keeps it recoverable and preserves its historical compliance score data. This field is only read during deletion and is not sent to the API on create or update. Defaults to `True`.
        /// </summary>
        [Input("hardDelete")]
        public Input<bool>? HardDelete { get; set; }

        /// <summary>
        /// Human-readable name for the tag rule.
        /// </summary>
        [Input("name", required: true)]
        public Input<string> Name { get; set; } = null!;

        /// <summary>
        /// When `True`, the rule matches tag values that do NOT match any of the supplied patterns. Defaults to `False`.
        /// </summary>
        [Input("negated")]
        public Input<bool>? Negated { get; set; }

        /// <summary>
        /// When `True`, telemetry without this tag is treated as a violation. Defaults to `False`.
        /// </summary>
        [Input("required")]
        public Input<bool>? Required { get; set; }

        /// <summary>
        /// How the rule is enforced. `Surfacing` only highlights non-compliant telemetry, while `Blocking` rejects telemetry that violates the rule. The API only accepts `Surfacing` at creation time, so creating a rule directly as `Blocking` requires `ForceBlockingOnCreate` to be set to `True`. Using `Blocking` at all requires blocking tag rules to be enabled for your organization; otherwise the API returns `403 permission denied`. Valid values are `Blocking`, `Surfacing`.
        /// </summary>
        [Input("ruleType", required: true)]
        public Input<string> RuleType { get; set; } = null!;

        /// <summary>
        /// The scope the rule applies within. Typically an environment, team, or organization-level identifier used to limit where the rule is enforced.
        /// </summary>
        [Input("scope", required: true)]
        public Input<string> Scope { get; set; } = null!;

        /// <summary>
        /// The telemetry source that the tag rule applies to. This field cannot be updated after creation; changing it forces a new resource. Valid values are `Logs`, `Spans`, `Metrics`, `Rum`, `Feed`.
        /// </summary>
        [Input("source", required: true)]
        public Input<string> Source { get; set; } = null!;

        /// <summary>
        /// The tag key that the rule governs (for example, `Service`).
        /// </summary>
        [Input("tagKey", required: true)]
        public Input<string> TagKey { get; set; } = null!;

        [Input("tagValuePatterns", required: true)]
        private InputList<string>? _tagValuePatterns;

        /// <summary>
        /// One or more patterns that valid values for the tag key must match. At least one pattern is required. These are not regular expressions: the API restricts pattern characters to `A-Za-z0-9_:-.,/*`, with `*` acting as a wildcard. A pattern made up only of wildcards (for example a bare `*`) is rejected by the API.
        /// </summary>
        public InputList<string> TagValuePatterns
        {
            get => _tagValuePatterns ?? (_tagValuePatterns = new InputList<string>());
            set => _tagValuePatterns = value;
        }

        public TagRuleArgs()
        {
        }
        public static new TagRuleArgs Empty => new TagRuleArgs();
    }

    public sealed class TagRuleState : global::Pulumi.ResourceArgs
    {
        /// <summary>
        /// The RFC 3339 timestamp at which the rule was created.
        /// </summary>
        [Input("createdAt")]
        public Input<string>? CreatedAt { get; set; }

        /// <summary>
        /// The identifier of the user who created the rule.
        /// </summary>
        [Input("createdBy")]
        public Input<string>? CreatedBy { get; set; }

        /// <summary>
        /// Whether the rule is currently enforced. Defaults to `True`.
        /// </summary>
        [Input("enabled")]
        public Input<bool>? Enabled { get; set; }

        /// <summary>
        /// Set to `True` to allow creating a rule with `RuleType` set to `Blocking`. The Datadog API only accepts `Surfacing` at creation time, so the provider creates the rule as `Surfacing` and then immediately updates it to `Blocking`, which makes the create non-atomic: if the update fails, a `Surfacing` rule is left behind and the resource is marked tainted. This field is only read during creation and is not sent to the API; changing it afterwards produces a diff that has no effect.
        /// </summary>
        [Input("forceBlockingOnCreate")]
        public Input<bool>? ForceBlockingOnCreate { get; set; }

        /// <summary>
        /// Whether destroying this resource permanently deletes the tag rule. When set to `False` the rule is soft-deleted instead, which keeps it recoverable and preserves its historical compliance score data. This field is only read during deletion and is not sent to the API on create or update. Defaults to `True`.
        /// </summary>
        [Input("hardDelete")]
        public Input<bool>? HardDelete { get; set; }

        /// <summary>
        /// The RFC 3339 timestamp at which the rule was last modified.
        /// </summary>
        [Input("modifiedAt")]
        public Input<string>? ModifiedAt { get; set; }

        /// <summary>
        /// The identifier of the user who last modified the rule.
        /// </summary>
        [Input("modifiedBy")]
        public Input<string>? ModifiedBy { get; set; }

        /// <summary>
        /// Human-readable name for the tag rule.
        /// </summary>
        [Input("name")]
        public Input<string>? Name { get; set; }

        /// <summary>
        /// When `True`, the rule matches tag values that do NOT match any of the supplied patterns. Defaults to `False`.
        /// </summary>
        [Input("negated")]
        public Input<bool>? Negated { get; set; }

        /// <summary>
        /// When `True`, telemetry without this tag is treated as a violation. Defaults to `False`.
        /// </summary>
        [Input("required")]
        public Input<bool>? Required { get; set; }

        /// <summary>
        /// How the rule is enforced. `Surfacing` only highlights non-compliant telemetry, while `Blocking` rejects telemetry that violates the rule. The API only accepts `Surfacing` at creation time, so creating a rule directly as `Blocking` requires `ForceBlockingOnCreate` to be set to `True`. Using `Blocking` at all requires blocking tag rules to be enabled for your organization; otherwise the API returns `403 permission denied`. Valid values are `Blocking`, `Surfacing`.
        /// </summary>
        [Input("ruleType")]
        public Input<string>? RuleType { get; set; }

        /// <summary>
        /// The scope the rule applies within. Typically an environment, team, or organization-level identifier used to limit where the rule is enforced.
        /// </summary>
        [Input("scope")]
        public Input<string>? Scope { get; set; }

        /// <summary>
        /// The telemetry source that the tag rule applies to. This field cannot be updated after creation; changing it forces a new resource. Valid values are `Logs`, `Spans`, `Metrics`, `Rum`, `Feed`.
        /// </summary>
        [Input("source")]
        public Input<string>? Source { get; set; }

        /// <summary>
        /// The tag key that the rule governs (for example, `Service`).
        /// </summary>
        [Input("tagKey")]
        public Input<string>? TagKey { get; set; }

        [Input("tagValuePatterns")]
        private InputList<string>? _tagValuePatterns;

        /// <summary>
        /// One or more patterns that valid values for the tag key must match. At least one pattern is required. These are not regular expressions: the API restricts pattern characters to `A-Za-z0-9_:-.,/*`, with `*` acting as a wildcard. A pattern made up only of wildcards (for example a bare `*`) is rejected by the API.
        /// </summary>
        public InputList<string> TagValuePatterns
        {
            get => _tagValuePatterns ?? (_tagValuePatterns = new InputList<string>());
            set => _tagValuePatterns = value;
        }

        /// <summary>
        /// A monotonically increasing version counter that is incremented on each update.
        /// </summary>
        [Input("version")]
        public Input<int>? Version { get; set; }

        public TagRuleState()
        {
        }
        public static new TagRuleState Empty => new TagRuleState();
    }
}
