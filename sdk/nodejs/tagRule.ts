// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Provides a Datadog tag rule resource. This can be used to create and manage Datadog governance tag rules.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as datadog from "@pulumi/datadog";
 *
 * // Create a surfacing tag rule, which highlights non-compliant telemetry
 * // without rejecting it.
 * const env = new datadog.TagRule("env", {
 *     name: "env must be a known environment",
 *     source: "logs",
 *     scope: "org",
 *     tagKey: "env",
 *     tagValuePatterns: [
 *         "prod",
 *         "staging",
 *         "dev",
 *     ],
 *     ruleType: "surfacing",
 *     required: true,
 * });
 * // Create a blocking tag rule, which rejects telemetry that violates it.
 * // The Datadog API only accepts `surfacing` at creation time, so the provider
 * // creates the rule and then immediately updates it to `blocking`. Because that
 * // makes the create non-atomic, it has to be opted into explicitly.
 * const service = new datadog.TagRule("service", {
 *     name: "service must be lowercase and hyphenated",
 *     source: "spans",
 *     scope: "org",
 *     tagKey: "service",
 *     tagValuePatterns: ["web-*"],
 *     ruleType: "blocking",
 *     forceBlockingOnCreate: true,
 *     hardDelete: false,
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * ```sh
 * $ pulumi import datadog:index/tagRule:TagRule env "<rule_id>"
 * ```
 */
export class TagRule extends pulumi.CustomResource {
    /**
     * Get an existing TagRule resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: TagRuleState, opts?: pulumi.CustomResourceOptions): TagRule {
        return new TagRule(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'datadog:index/tagRule:TagRule';

    /**
     * Returns true if the given object is an instance of TagRule.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is TagRule {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === TagRule.__pulumiType;
    }

    /**
     * The RFC 3339 timestamp at which the rule was created.
     */
    declare public /*out*/ readonly createdAt: pulumi.Output<string>;
    /**
     * The identifier of the user who created the rule.
     */
    declare public /*out*/ readonly createdBy: pulumi.Output<string>;
    /**
     * Whether the rule is currently enforced. Defaults to `true`.
     */
    declare public readonly enabled: pulumi.Output<boolean>;
    /**
     * Set to `true` to allow creating a rule with `ruleType` set to `blocking`. The Datadog API only accepts `surfacing` at creation time, so the provider creates the rule as `surfacing` and then immediately updates it to `blocking`, which makes the create non-atomic: if the update fails, a `surfacing` rule is left behind and the resource is marked tainted. This field is only read during creation and is not sent to the API; changing it afterwards produces a diff that has no effect.
     */
    declare public readonly forceBlockingOnCreate: pulumi.Output<boolean | undefined>;
    /**
     * Whether destroying this resource permanently deletes the tag rule. When set to `false` the rule is soft-deleted instead, which keeps it recoverable and preserves its historical compliance score data. This field is only read during deletion and is not sent to the API on create or update. Defaults to `true`.
     */
    declare public readonly hardDelete: pulumi.Output<boolean>;
    /**
     * The RFC 3339 timestamp at which the rule was last modified.
     */
    declare public /*out*/ readonly modifiedAt: pulumi.Output<string>;
    /**
     * The identifier of the user who last modified the rule.
     */
    declare public /*out*/ readonly modifiedBy: pulumi.Output<string>;
    /**
     * Human-readable name for the tag rule.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * When `true`, the rule matches tag values that do NOT match any of the supplied patterns. Defaults to `false`.
     */
    declare public readonly negated: pulumi.Output<boolean>;
    /**
     * When `true`, telemetry without this tag is treated as a violation. Defaults to `false`.
     */
    declare public readonly required: pulumi.Output<boolean>;
    /**
     * How the rule is enforced. `surfacing` only highlights non-compliant telemetry, while `blocking` rejects telemetry that violates the rule. The API only accepts `surfacing` at creation time, so creating a rule directly as `blocking` requires `forceBlockingOnCreate` to be set to `true`. Using `blocking` at all requires blocking tag rules to be enabled for your organization; otherwise the API returns `403 permission denied`. Valid values are `blocking`, `surfacing`.
     */
    declare public readonly ruleType: pulumi.Output<string>;
    /**
     * The scope the rule applies within. Typically an environment, team, or organization-level identifier used to limit where the rule is enforced.
     */
    declare public readonly scope: pulumi.Output<string>;
    /**
     * The telemetry source that the tag rule applies to. This field cannot be updated after creation; changing it forces a new resource. Valid values are `logs`, `spans`, `metrics`, `rum`, `feed`.
     */
    declare public readonly source: pulumi.Output<string>;
    /**
     * The tag key that the rule governs (for example, `service`).
     */
    declare public readonly tagKey: pulumi.Output<string>;
    /**
     * One or more patterns that valid values for the tag key must match. At least one pattern is required. These are not regular expressions: the API restricts pattern characters to `A-Za-z0-9_:-.,/*`, with `*` acting as a wildcard. A pattern made up only of wildcards (for example a bare `*`) is rejected by the API.
     */
    declare public readonly tagValuePatterns: pulumi.Output<string[]>;
    /**
     * A monotonically increasing version counter that is incremented on each update.
     */
    declare public /*out*/ readonly version: pulumi.Output<number>;

    /**
     * Create a TagRule resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: TagRuleArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: TagRuleArgs | TagRuleState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as TagRuleState | undefined;
            resourceInputs["createdAt"] = state?.createdAt;
            resourceInputs["createdBy"] = state?.createdBy;
            resourceInputs["enabled"] = state?.enabled;
            resourceInputs["forceBlockingOnCreate"] = state?.forceBlockingOnCreate;
            resourceInputs["hardDelete"] = state?.hardDelete;
            resourceInputs["modifiedAt"] = state?.modifiedAt;
            resourceInputs["modifiedBy"] = state?.modifiedBy;
            resourceInputs["name"] = state?.name;
            resourceInputs["negated"] = state?.negated;
            resourceInputs["required"] = state?.required;
            resourceInputs["ruleType"] = state?.ruleType;
            resourceInputs["scope"] = state?.scope;
            resourceInputs["source"] = state?.source;
            resourceInputs["tagKey"] = state?.tagKey;
            resourceInputs["tagValuePatterns"] = state?.tagValuePatterns;
            resourceInputs["version"] = state?.version;
        } else {
            const args = argsOrState as TagRuleArgs | undefined;
            if (args?.name === undefined && !opts.urn) {
                throw new Error("Missing required property 'name'");
            }
            if (args?.ruleType === undefined && !opts.urn) {
                throw new Error("Missing required property 'ruleType'");
            }
            if (args?.scope === undefined && !opts.urn) {
                throw new Error("Missing required property 'scope'");
            }
            if (args?.source === undefined && !opts.urn) {
                throw new Error("Missing required property 'source'");
            }
            if (args?.tagKey === undefined && !opts.urn) {
                throw new Error("Missing required property 'tagKey'");
            }
            if (args?.tagValuePatterns === undefined && !opts.urn) {
                throw new Error("Missing required property 'tagValuePatterns'");
            }
            resourceInputs["enabled"] = args?.enabled;
            resourceInputs["forceBlockingOnCreate"] = args?.forceBlockingOnCreate;
            resourceInputs["hardDelete"] = args?.hardDelete;
            resourceInputs["name"] = args?.name;
            resourceInputs["negated"] = args?.negated;
            resourceInputs["required"] = args?.required;
            resourceInputs["ruleType"] = args?.ruleType;
            resourceInputs["scope"] = args?.scope;
            resourceInputs["source"] = args?.source;
            resourceInputs["tagKey"] = args?.tagKey;
            resourceInputs["tagValuePatterns"] = args?.tagValuePatterns;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["createdBy"] = undefined /*out*/;
            resourceInputs["modifiedAt"] = undefined /*out*/;
            resourceInputs["modifiedBy"] = undefined /*out*/;
            resourceInputs["version"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(TagRule.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering TagRule resources.
 */
export interface TagRuleState {
    /**
     * The RFC 3339 timestamp at which the rule was created.
     */
    createdAt?: pulumi.Input<string | undefined>;
    /**
     * The identifier of the user who created the rule.
     */
    createdBy?: pulumi.Input<string | undefined>;
    /**
     * Whether the rule is currently enforced. Defaults to `true`.
     */
    enabled?: pulumi.Input<boolean | undefined>;
    /**
     * Set to `true` to allow creating a rule with `ruleType` set to `blocking`. The Datadog API only accepts `surfacing` at creation time, so the provider creates the rule as `surfacing` and then immediately updates it to `blocking`, which makes the create non-atomic: if the update fails, a `surfacing` rule is left behind and the resource is marked tainted. This field is only read during creation and is not sent to the API; changing it afterwards produces a diff that has no effect.
     */
    forceBlockingOnCreate?: pulumi.Input<boolean | undefined>;
    /**
     * Whether destroying this resource permanently deletes the tag rule. When set to `false` the rule is soft-deleted instead, which keeps it recoverable and preserves its historical compliance score data. This field is only read during deletion and is not sent to the API on create or update. Defaults to `true`.
     */
    hardDelete?: pulumi.Input<boolean | undefined>;
    /**
     * The RFC 3339 timestamp at which the rule was last modified.
     */
    modifiedAt?: pulumi.Input<string | undefined>;
    /**
     * The identifier of the user who last modified the rule.
     */
    modifiedBy?: pulumi.Input<string | undefined>;
    /**
     * Human-readable name for the tag rule.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * When `true`, the rule matches tag values that do NOT match any of the supplied patterns. Defaults to `false`.
     */
    negated?: pulumi.Input<boolean | undefined>;
    /**
     * When `true`, telemetry without this tag is treated as a violation. Defaults to `false`.
     */
    required?: pulumi.Input<boolean | undefined>;
    /**
     * How the rule is enforced. `surfacing` only highlights non-compliant telemetry, while `blocking` rejects telemetry that violates the rule. The API only accepts `surfacing` at creation time, so creating a rule directly as `blocking` requires `forceBlockingOnCreate` to be set to `true`. Using `blocking` at all requires blocking tag rules to be enabled for your organization; otherwise the API returns `403 permission denied`. Valid values are `blocking`, `surfacing`.
     */
    ruleType?: pulumi.Input<string | undefined>;
    /**
     * The scope the rule applies within. Typically an environment, team, or organization-level identifier used to limit where the rule is enforced.
     */
    scope?: pulumi.Input<string | undefined>;
    /**
     * The telemetry source that the tag rule applies to. This field cannot be updated after creation; changing it forces a new resource. Valid values are `logs`, `spans`, `metrics`, `rum`, `feed`.
     */
    source?: pulumi.Input<string | undefined>;
    /**
     * The tag key that the rule governs (for example, `service`).
     */
    tagKey?: pulumi.Input<string | undefined>;
    /**
     * One or more patterns that valid values for the tag key must match. At least one pattern is required. These are not regular expressions: the API restricts pattern characters to `A-Za-z0-9_:-.,/*`, with `*` acting as a wildcard. A pattern made up only of wildcards (for example a bare `*`) is rejected by the API.
     */
    tagValuePatterns?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * A monotonically increasing version counter that is incremented on each update.
     */
    version?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a TagRule resource.
 */
export interface TagRuleArgs {
    /**
     * Whether the rule is currently enforced. Defaults to `true`.
     */
    enabled?: pulumi.Input<boolean | undefined>;
    /**
     * Set to `true` to allow creating a rule with `ruleType` set to `blocking`. The Datadog API only accepts `surfacing` at creation time, so the provider creates the rule as `surfacing` and then immediately updates it to `blocking`, which makes the create non-atomic: if the update fails, a `surfacing` rule is left behind and the resource is marked tainted. This field is only read during creation and is not sent to the API; changing it afterwards produces a diff that has no effect.
     */
    forceBlockingOnCreate?: pulumi.Input<boolean | undefined>;
    /**
     * Whether destroying this resource permanently deletes the tag rule. When set to `false` the rule is soft-deleted instead, which keeps it recoverable and preserves its historical compliance score data. This field is only read during deletion and is not sent to the API on create or update. Defaults to `true`.
     */
    hardDelete?: pulumi.Input<boolean | undefined>;
    /**
     * Human-readable name for the tag rule.
     */
    name: pulumi.Input<string>;
    /**
     * When `true`, the rule matches tag values that do NOT match any of the supplied patterns. Defaults to `false`.
     */
    negated?: pulumi.Input<boolean | undefined>;
    /**
     * When `true`, telemetry without this tag is treated as a violation. Defaults to `false`.
     */
    required?: pulumi.Input<boolean | undefined>;
    /**
     * How the rule is enforced. `surfacing` only highlights non-compliant telemetry, while `blocking` rejects telemetry that violates the rule. The API only accepts `surfacing` at creation time, so creating a rule directly as `blocking` requires `forceBlockingOnCreate` to be set to `true`. Using `blocking` at all requires blocking tag rules to be enabled for your organization; otherwise the API returns `403 permission denied`. Valid values are `blocking`, `surfacing`.
     */
    ruleType: pulumi.Input<string>;
    /**
     * The scope the rule applies within. Typically an environment, team, or organization-level identifier used to limit where the rule is enforced.
     */
    scope: pulumi.Input<string>;
    /**
     * The telemetry source that the tag rule applies to. This field cannot be updated after creation; changing it forces a new resource. Valid values are `logs`, `spans`, `metrics`, `rum`, `feed`.
     */
    source: pulumi.Input<string>;
    /**
     * The tag key that the rule governs (for example, `service`).
     */
    tagKey: pulumi.Input<string>;
    /**
     * One or more patterns that valid values for the tag key must match. At least one pattern is required. These are not regular expressions: the API restricts pattern characters to `A-Za-z0-9_:-.,/*`, with `*` acting as a wildcard. A pattern made up only of wildcards (for example a bare `*`) is rejected by the API.
     */
    tagValuePatterns: pulumi.Input<pulumi.Input<string>[]>;
}
