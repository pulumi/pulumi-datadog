// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Provides a Datadog Team Connection resource. This can be used to create and manage connections between a Datadog team and an external team (e.g. GitHub).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as datadog from "@pulumi/datadog";
 *
 * const example = new datadog.Team("example", {
 *     description: "Example team",
 *     handle: "example-team",
 *     name: "Example Team",
 * });
 * // Create a connection between a Datadog team and a GitHub team
 * const exampleTeamConnection = new datadog.TeamConnection("example", {
 *     team: [{
 *         id: example.id,
 *         type: "team",
 *     }],
 *     connectedTeam: [{
 *         id: "@GitHubOrg/team-handle",
 *         type: "github_team",
 *     }],
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * ```sh
 * $ pulumi import datadog:index/teamConnection:TeamConnection example "${connection_id}"
 * ```
 */
export class TeamConnection extends pulumi.CustomResource {
    /**
     * Get an existing TeamConnection resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: TeamConnectionState, opts?: pulumi.CustomResourceOptions): TeamConnection {
        return new TeamConnection(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'datadog:index/teamConnection:TeamConnection';

    /**
     * Returns true if the given object is an instance of TeamConnection.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is TeamConnection {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === TeamConnection.__pulumiType;
    }

    /**
     * The external connected team reference (e.g. a GitHub team).
     */
    declare public readonly connectedTeam: pulumi.Output<outputs.TeamConnectionConnectedTeam>;
    /**
     * The source of the connection (e.g. github).
     */
    declare public readonly source: pulumi.Output<string>;
    /**
     * The Datadog team reference.
     */
    declare public readonly team: pulumi.Output<outputs.TeamConnectionTeam>;

    /**
     * Create a TeamConnection resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: TeamConnectionArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: TeamConnectionArgs | TeamConnectionState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as TeamConnectionState | undefined;
            resourceInputs["connectedTeam"] = state?.connectedTeam;
            resourceInputs["source"] = state?.source;
            resourceInputs["team"] = state?.team;
        } else {
            const args = argsOrState as TeamConnectionArgs | undefined;
            if (args?.connectedTeam === undefined && !opts.urn) {
                throw new Error("Missing required property 'connectedTeam'");
            }
            if (args?.team === undefined && !opts.urn) {
                throw new Error("Missing required property 'team'");
            }
            resourceInputs["connectedTeam"] = args?.connectedTeam;
            resourceInputs["source"] = args?.source;
            resourceInputs["team"] = args?.team;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(TeamConnection.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering TeamConnection resources.
 */
export interface TeamConnectionState {
    /**
     * The external connected team reference (e.g. a GitHub team).
     */
    connectedTeam?: pulumi.Input<inputs.TeamConnectionConnectedTeam | undefined>;
    /**
     * The source of the connection (e.g. github).
     */
    source?: pulumi.Input<string | undefined>;
    /**
     * The Datadog team reference.
     */
    team?: pulumi.Input<inputs.TeamConnectionTeam | undefined>;
}

/**
 * The set of arguments for constructing a TeamConnection resource.
 */
export interface TeamConnectionArgs {
    /**
     * The external connected team reference (e.g. a GitHub team).
     */
    connectedTeam: pulumi.Input<inputs.TeamConnectionConnectedTeam>;
    /**
     * The source of the connection (e.g. github).
     */
    source?: pulumi.Input<string | undefined>;
    /**
     * The Datadog team reference.
     */
    team: pulumi.Input<inputs.TeamConnectionTeam>;
}
