// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Provides a Datadog security findings default inbox rule resource. Datadog manages default inbox rules. Their name, rule, and action are read-only, and each organization can change only whether they are enabled. Default inbox rules always exist, so this resource can only be imported, not created; removing it from the configuration does not delete the rule, it only stops managing it. Default inbox rules are not part of the evaluation order managed by the `datadog.SecurityFindingsInboxRulesOrder` resource.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as datadog from "@pulumi/datadog";
 *
 * // Default inbox rules always exist server-side and can only be imported, not created.
 * // Only `enabled` can be managed; all other attributes are read from Datadog.
 * // Import the rule with its fixed ID before applying, e.g.:
 * //   terraform import datadog_security_findings_default_inbox_rule.secret secret_default_rule
 * // Known default rule IDs: identity_risk_default_rule, secret_default_rule,
 * // library_vulnerability_default_rule, attack_path_default_rule,
 * // host_and_container_vulnerability_default_rule, runtime_code_vulnerability_default_rule,
 * // iac_misconfiguration_default_rule, misconfiguration_default_rule.
 * const secret = new datadog.SecurityFindingsDefaultInboxRule("secret", {enabled: true});
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * ```sh
 * $ pulumi import datadog:index/securityFindingsDefaultInboxRule:SecurityFindingsDefaultInboxRule secret secret_default_rule
 * ```
 */
export class SecurityFindingsDefaultInboxRule extends pulumi.CustomResource {
    /**
     * Get an existing SecurityFindingsDefaultInboxRule resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SecurityFindingsDefaultInboxRuleState, opts?: pulumi.CustomResourceOptions): SecurityFindingsDefaultInboxRule {
        return new SecurityFindingsDefaultInboxRule(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'datadog:index/securityFindingsDefaultInboxRule:SecurityFindingsDefaultInboxRule';

    /**
     * Returns true if the given object is an instance of SecurityFindingsDefaultInboxRule.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SecurityFindingsDefaultInboxRule {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SecurityFindingsDefaultInboxRule.__pulumiType;
    }

    /**
     * The action to take when the inbox rule matches a finding.
     */
    declare public /*out*/ readonly action: pulumi.Output<outputs.SecurityFindingsDefaultInboxRuleAction>;
    /**
     * Whether the default inbox rule is enabled for the organization. When not set, the rule's current server-side value is adopted; declare it to manage the value. Default inbox rules are enabled unless disabled.
     */
    declare public readonly enabled: pulumi.Output<boolean>;
    /**
     * The name of the default inbox rule.
     */
    declare public /*out*/ readonly name: pulumi.Output<string>;
    /**
     * Defines the scope of findings to which the automation rule applies.
     */
    declare public /*out*/ readonly rule: pulumi.Output<outputs.SecurityFindingsDefaultInboxRuleRule>;

    /**
     * Create a SecurityFindingsDefaultInboxRule resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: SecurityFindingsDefaultInboxRuleArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SecurityFindingsDefaultInboxRuleArgs | SecurityFindingsDefaultInboxRuleState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SecurityFindingsDefaultInboxRuleState | undefined;
            resourceInputs["action"] = state?.action;
            resourceInputs["enabled"] = state?.enabled;
            resourceInputs["name"] = state?.name;
            resourceInputs["rule"] = state?.rule;
        } else {
            const args = argsOrState as SecurityFindingsDefaultInboxRuleArgs | undefined;
            resourceInputs["enabled"] = args?.enabled;
            resourceInputs["action"] = undefined /*out*/;
            resourceInputs["name"] = undefined /*out*/;
            resourceInputs["rule"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SecurityFindingsDefaultInboxRule.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SecurityFindingsDefaultInboxRule resources.
 */
export interface SecurityFindingsDefaultInboxRuleState {
    /**
     * The action to take when the inbox rule matches a finding.
     */
    action?: pulumi.Input<inputs.SecurityFindingsDefaultInboxRuleAction | undefined>;
    /**
     * Whether the default inbox rule is enabled for the organization. When not set, the rule's current server-side value is adopted; declare it to manage the value. Default inbox rules are enabled unless disabled.
     */
    enabled?: pulumi.Input<boolean | undefined>;
    /**
     * The name of the default inbox rule.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Defines the scope of findings to which the automation rule applies.
     */
    rule?: pulumi.Input<inputs.SecurityFindingsDefaultInboxRuleRule | undefined>;
}

/**
 * The set of arguments for constructing a SecurityFindingsDefaultInboxRule resource.
 */
export interface SecurityFindingsDefaultInboxRuleArgs {
    /**
     * Whether the default inbox rule is enabled for the organization. When not set, the rule's current server-side value is adopted; declare it to manage the value. Default inbox rules are enabled unless disabled.
     */
    enabled?: pulumi.Input<boolean | undefined>;
}
