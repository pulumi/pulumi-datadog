// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Provides a Datadog Fleet Automation Agent upgrade schedule. Schedule mutations use Preview API endpoints and require an application key with the `agentUpgradeWrite` and `hostsRead` permissions.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as datadog from "@pulumi/datadog";
 *
 * // Fleet schedule mutations use Preview API endpoints. The application key must
 * // have both the agent_upgrade_write and hosts_read permissions.
 * const nonproduction = new datadog.FleetSchedule("nonproduction", {
 *     name: "Non-production Agent upgrades",
 *     query: "env:staging service:example",
 *     status: "inactive",
 *     versionToLatest: 1,
 *     rule: {
 *         days_of_week: [
 *             "Tue",
 *             "Thu",
 *         ],
 *         maintenance_window_duration: 120,
 *         start_maintenance_window: "03:00",
 *         timezone: "America/New_York",
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * ```sh
 * $ pulumi import datadog:index/fleetSchedule:FleetSchedule nonproduction "<schedule_id>"
 * ```
 */
export class FleetSchedule extends pulumi.CustomResource {
    /**
     * Get an existing FleetSchedule resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: FleetScheduleState, opts?: pulumi.CustomResourceOptions): FleetSchedule {
        return new FleetSchedule(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'datadog:index/fleetSchedule:FleetSchedule';

    /**
     * Returns true if the given object is an instance of FleetSchedule.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is FleetSchedule {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === FleetSchedule.__pulumiType;
    }

    /**
     * Human-readable name for the schedule. String length must be at least 1.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Datadog host query used to select the Agent upgrade targets. String length must be at least 1.
     */
    declare public readonly query: pulumi.Output<string>;
    /**
     * Weekly recurrence and maintenance-window configuration for the schedule.
     */
    declare public readonly rule: pulumi.Output<outputs.FleetScheduleRule>;
    /**
     * Whether the schedule creates deployments. Valid values are `active` and `inactive`. The API default is used when omitted. Valid values are `active`, `inactive`.
     */
    declare public readonly status: pulumi.Output<string>;
    /**
     * Number of major Agent versions behind the latest version to target: `0`, `1`, or `2`. The API default is used when omitted. Value must be between 0 and 2.
     */
    declare public readonly versionToLatest: pulumi.Output<number>;

    /**
     * Create a FleetSchedule resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: FleetScheduleArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: FleetScheduleArgs | FleetScheduleState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as FleetScheduleState | undefined;
            resourceInputs["name"] = state?.name;
            resourceInputs["query"] = state?.query;
            resourceInputs["rule"] = state?.rule;
            resourceInputs["status"] = state?.status;
            resourceInputs["versionToLatest"] = state?.versionToLatest;
        } else {
            const args = argsOrState as FleetScheduleArgs | undefined;
            if (args?.name === undefined && !opts.urn) {
                throw new Error("Missing required property 'name'");
            }
            if (args?.query === undefined && !opts.urn) {
                throw new Error("Missing required property 'query'");
            }
            if (args?.rule === undefined && !opts.urn) {
                throw new Error("Missing required property 'rule'");
            }
            resourceInputs["name"] = args?.name;
            resourceInputs["query"] = args?.query;
            resourceInputs["rule"] = args?.rule;
            resourceInputs["status"] = args?.status;
            resourceInputs["versionToLatest"] = args?.versionToLatest;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(FleetSchedule.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering FleetSchedule resources.
 */
export interface FleetScheduleState {
    /**
     * Human-readable name for the schedule. String length must be at least 1.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Datadog host query used to select the Agent upgrade targets. String length must be at least 1.
     */
    query?: pulumi.Input<string | undefined>;
    /**
     * Weekly recurrence and maintenance-window configuration for the schedule.
     */
    rule?: pulumi.Input<inputs.FleetScheduleRule | undefined>;
    /**
     * Whether the schedule creates deployments. Valid values are `active` and `inactive`. The API default is used when omitted. Valid values are `active`, `inactive`.
     */
    status?: pulumi.Input<string | undefined>;
    /**
     * Number of major Agent versions behind the latest version to target: `0`, `1`, or `2`. The API default is used when omitted. Value must be between 0 and 2.
     */
    versionToLatest?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a FleetSchedule resource.
 */
export interface FleetScheduleArgs {
    /**
     * Human-readable name for the schedule. String length must be at least 1.
     */
    name: pulumi.Input<string>;
    /**
     * Datadog host query used to select the Agent upgrade targets. String length must be at least 1.
     */
    query: pulumi.Input<string>;
    /**
     * Weekly recurrence and maintenance-window configuration for the schedule.
     */
    rule: pulumi.Input<inputs.FleetScheduleRule>;
    /**
     * Whether the schedule creates deployments. Valid values are `active` and `inactive`. The API default is used when omitted. Valid values are `active`, `inactive`.
     */
    status?: pulumi.Input<string | undefined>;
    /**
     * Number of major Agent versions behind the latest version to target: `0`, `1`, or `2`. The API default is used when omitted. Value must be between 0 and 2.
     */
    versionToLatest?: pulumi.Input<number | undefined>;
}
