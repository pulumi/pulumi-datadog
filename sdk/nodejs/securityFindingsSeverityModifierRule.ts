// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Provides a Datadog security findings automation severity modifier rule resource. This can be used to create and manage rules that automatically adjust the severity of matching security findings. Use the `datadog.SecurityFindingsSeverityModifierRulesOrder` resource to manage the evaluation order of severity modifier rules.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as datadog from "@pulumi/datadog";
 *
 * // Create a new severity modifier rule that raises the severity of secrets found in production.
 * const escalateProdSecrets = new datadog.SecurityFindingsSeverityModifierRule("escalate_prod_secrets", {
 *     name: "Escalate secrets found in production",
 *     enabled: true,
 *     rule: {
 *         finding_types: ["secret"],
 *         query: "env:prod",
 *     },
 *     action: {
 *         set: {
 *             severity: "critical",
 *             description: "Secrets in production are always treated as critical.",
 *         },
 *     },
 * });
 * // Create a severity modifier rule that shifts the severity of a finding down by one rank.
 * const deprioritizeDevMisconfigurations = new datadog.SecurityFindingsSeverityModifierRule("deprioritize_dev_misconfigurations", {
 *     name: "Deprioritize misconfigurations in dev",
 *     enabled: true,
 *     rule: {
 *         finding_types: ["misconfiguration"],
 *         query: "env:dev",
 *     },
 *     action: {
 *         shift: {
 *             severityDelta: "down_one",
 *             description: "Misconfigurations in dev are lower priority.",
 *         },
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * ```sh
 * $ pulumi import datadog:index/securityFindingsSeverityModifierRule:SecurityFindingsSeverityModifierRule escalate_prod_secrets "00000000-0000-0000-0000-000000000000"
 * ```
 */
export class SecurityFindingsSeverityModifierRule extends pulumi.CustomResource {
    /**
     * Get an existing SecurityFindingsSeverityModifierRule resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SecurityFindingsSeverityModifierRuleState, opts?: pulumi.CustomResourceOptions): SecurityFindingsSeverityModifierRule {
        return new SecurityFindingsSeverityModifierRule(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'datadog:index/securityFindingsSeverityModifierRule:SecurityFindingsSeverityModifierRule';

    /**
     * Returns true if the given object is an instance of SecurityFindingsSeverityModifierRule.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SecurityFindingsSeverityModifierRule {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SecurityFindingsSeverityModifierRule.__pulumiType;
    }

    /**
     * The action to take when a severity modifier rule matches a finding. This is a discriminated union on `type`: `set` assigns a fixed severity, while `shift` moves the severity up or down by one severity rank. In this resource the union is expressed as the `set` and `shift` blocks; exactly one must be provided. A severity modifier rule's `rule.query` must not filter on `@severity` or on the `@severity_details.user_adjusted.*` namespace. Use `@severity_details.adjusted.value` instead, which reflects the severity before user-defined adjustments.
     */
    declare public readonly action: pulumi.Output<outputs.SecurityFindingsSeverityModifierRuleAction>;
    /**
     * Whether the severity modifier rule is enabled. Defaults to `true`.
     */
    declare public readonly enabled: pulumi.Output<boolean>;
    /**
     * The name of the severity modifier rule.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Defines the scope of findings to which the automation rule applies.
     */
    declare public readonly rule: pulumi.Output<outputs.SecurityFindingsSeverityModifierRuleRule>;

    /**
     * Create a SecurityFindingsSeverityModifierRule resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SecurityFindingsSeverityModifierRuleArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SecurityFindingsSeverityModifierRuleArgs | SecurityFindingsSeverityModifierRuleState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SecurityFindingsSeverityModifierRuleState | undefined;
            resourceInputs["action"] = state?.action;
            resourceInputs["enabled"] = state?.enabled;
            resourceInputs["name"] = state?.name;
            resourceInputs["rule"] = state?.rule;
        } else {
            const args = argsOrState as SecurityFindingsSeverityModifierRuleArgs | undefined;
            if (args?.action === undefined && !opts.urn) {
                throw new Error("Missing required property 'action'");
            }
            if (args?.name === undefined && !opts.urn) {
                throw new Error("Missing required property 'name'");
            }
            if (args?.rule === undefined && !opts.urn) {
                throw new Error("Missing required property 'rule'");
            }
            resourceInputs["action"] = args?.action;
            resourceInputs["enabled"] = args?.enabled;
            resourceInputs["name"] = args?.name;
            resourceInputs["rule"] = args?.rule;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SecurityFindingsSeverityModifierRule.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SecurityFindingsSeverityModifierRule resources.
 */
export interface SecurityFindingsSeverityModifierRuleState {
    /**
     * The action to take when a severity modifier rule matches a finding. This is a discriminated union on `type`: `set` assigns a fixed severity, while `shift` moves the severity up or down by one severity rank. In this resource the union is expressed as the `set` and `shift` blocks; exactly one must be provided. A severity modifier rule's `rule.query` must not filter on `@severity` or on the `@severity_details.user_adjusted.*` namespace. Use `@severity_details.adjusted.value` instead, which reflects the severity before user-defined adjustments.
     */
    action?: pulumi.Input<inputs.SecurityFindingsSeverityModifierRuleAction | undefined>;
    /**
     * Whether the severity modifier rule is enabled. Defaults to `true`.
     */
    enabled?: pulumi.Input<boolean | undefined>;
    /**
     * The name of the severity modifier rule.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Defines the scope of findings to which the automation rule applies.
     */
    rule?: pulumi.Input<inputs.SecurityFindingsSeverityModifierRuleRule | undefined>;
}

/**
 * The set of arguments for constructing a SecurityFindingsSeverityModifierRule resource.
 */
export interface SecurityFindingsSeverityModifierRuleArgs {
    /**
     * The action to take when a severity modifier rule matches a finding. This is a discriminated union on `type`: `set` assigns a fixed severity, while `shift` moves the severity up or down by one severity rank. In this resource the union is expressed as the `set` and `shift` blocks; exactly one must be provided. A severity modifier rule's `rule.query` must not filter on `@severity` or on the `@severity_details.user_adjusted.*` namespace. Use `@severity_details.adjusted.value` instead, which reflects the severity before user-defined adjustments.
     */
    action: pulumi.Input<inputs.SecurityFindingsSeverityModifierRuleAction>;
    /**
     * Whether the severity modifier rule is enabled. Defaults to `true`.
     */
    enabled?: pulumi.Input<boolean | undefined>;
    /**
     * The name of the severity modifier rule.
     */
    name: pulumi.Input<string>;
    /**
     * Defines the scope of findings to which the automation rule applies.
     */
    rule: pulumi.Input<inputs.SecurityFindingsSeverityModifierRuleRule>;
}
