// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Provides a Datadog Team Sync resource. This can be used to configure team synchronization from external sources (e.g. GitHub) into Datadog.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as datadog from "@pulumi/datadog";
 *
 * // Configure team synchronization from GitHub
 * const example = new datadog.TeamSync("example", {
 *     source: "github",
 *     type: "link",
 *     frequency: "continuously",
 *     syncMembership: true,
 *     selectionStates: [{
 *         operation: "include",
 *         scope: "subtree",
 *         externalId: [{
 *             type: "organization",
 *             value: "12345",
 *         }],
 *     }],
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * ```sh
 * $ pulumi import datadog:index/teamSync:TeamSync example "github"
 * ```
 */
export class TeamSync extends pulumi.CustomResource {
    /**
     * Get an existing TeamSync resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: TeamSyncState, opts?: pulumi.CustomResourceOptions): TeamSync {
        return new TeamSync(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'datadog:index/teamSync:TeamSync';

    /**
     * Returns true if the given object is an instance of TeamSync.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is TeamSync {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === TeamSync.__pulumiType;
    }

    /**
     * How often the sync process should run. Valid values are `once`, `continuously`, `paused`. Defaults to `"once"`.
     */
    declare public readonly frequency: pulumi.Output<string>;
    /**
     * Specifies which teams or organizations to sync. When provided, synchronization is limited to the specified items and their subtrees.
     */
    declare public readonly selectionStates: pulumi.Output<outputs.TeamSyncSelectionState[] | undefined>;
    /**
     * The external source platform for team synchronization. Valid values are `github`.
     */
    declare public readonly source: pulumi.Output<string>;
    /**
     * Whether to sync members from the external team to the Datadog team. Defaults to `false`.
     */
    declare public readonly syncMembership: pulumi.Output<boolean>;
    /**
     * The type of synchronization operation. `link` connects teams by matching names. `provision` creates new teams when no match is found. Valid values are `link`, `provision`.
     */
    declare public readonly type: pulumi.Output<string>;

    /**
     * Create a TeamSync resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: TeamSyncArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: TeamSyncArgs | TeamSyncState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as TeamSyncState | undefined;
            resourceInputs["frequency"] = state?.frequency;
            resourceInputs["selectionStates"] = state?.selectionStates;
            resourceInputs["source"] = state?.source;
            resourceInputs["syncMembership"] = state?.syncMembership;
            resourceInputs["type"] = state?.type;
        } else {
            const args = argsOrState as TeamSyncArgs | undefined;
            if (args?.source === undefined && !opts.urn) {
                throw new Error("Missing required property 'source'");
            }
            if (args?.type === undefined && !opts.urn) {
                throw new Error("Missing required property 'type'");
            }
            resourceInputs["frequency"] = args?.frequency;
            resourceInputs["selectionStates"] = args?.selectionStates;
            resourceInputs["source"] = args?.source;
            resourceInputs["syncMembership"] = args?.syncMembership;
            resourceInputs["type"] = args?.type;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(TeamSync.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering TeamSync resources.
 */
export interface TeamSyncState {
    /**
     * How often the sync process should run. Valid values are `once`, `continuously`, `paused`. Defaults to `"once"`.
     */
    frequency?: pulumi.Input<string | undefined>;
    /**
     * Specifies which teams or organizations to sync. When provided, synchronization is limited to the specified items and their subtrees.
     */
    selectionStates?: pulumi.Input<pulumi.Input<inputs.TeamSyncSelectionState>[] | undefined>;
    /**
     * The external source platform for team synchronization. Valid values are `github`.
     */
    source?: pulumi.Input<string | undefined>;
    /**
     * Whether to sync members from the external team to the Datadog team. Defaults to `false`.
     */
    syncMembership?: pulumi.Input<boolean | undefined>;
    /**
     * The type of synchronization operation. `link` connects teams by matching names. `provision` creates new teams when no match is found. Valid values are `link`, `provision`.
     */
    type?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a TeamSync resource.
 */
export interface TeamSyncArgs {
    /**
     * How often the sync process should run. Valid values are `once`, `continuously`, `paused`. Defaults to `"once"`.
     */
    frequency?: pulumi.Input<string | undefined>;
    /**
     * Specifies which teams or organizations to sync. When provided, synchronization is limited to the specified items and their subtrees.
     */
    selectionStates?: pulumi.Input<pulumi.Input<inputs.TeamSyncSelectionState>[] | undefined>;
    /**
     * The external source platform for team synchronization. Valid values are `github`.
     */
    source: pulumi.Input<string>;
    /**
     * Whether to sync members from the external team to the Datadog team. Defaults to `false`.
     */
    syncMembership?: pulumi.Input<boolean | undefined>;
    /**
     * The type of synchronization operation. `link` connects teams by matching names. `provision` creates new teams when no match is found. Valid values are `link`, `provision`.
     */
    type: pulumi.Input<string>;
}
