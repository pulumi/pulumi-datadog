// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Provides a Datadog Cost Custom Forecast resource. This resource manages the custom forecast override entries for a `datadog.CostBudget`. **Note:** each entry's `(month, tag_filters)` combination must correspond to an existing entry on the referenced budget, and the budget must exist before this resource is created.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as datadog from "@pulumi/datadog";
 *
 * // A budget with two monthly entries for the same tag combination, plus a
 * // custom forecast override for the first month. The entry's (month, tag_filters)
 * // must match one of the budget's own entries.
 * const example = new datadog.CostBudget("example", {
 *     name: "Engineering Q1 Budget",
 *     metricsQuery: "sum:aws.cost.amortized{service:ec2} by {service}",
 *     startMonth: 202601,
 *     endMonth: 202602,
 *     entries: [
 *         {
 *             amount: 1000,
 *             month: 202601,
 *             tagFilters: [{
 *                 tagKey: "service",
 *                 tagValue: "ec2",
 *             }],
 *         },
 *         {
 *             amount: 1200,
 *             month: 202602,
 *             tagFilters: [{
 *                 tagKey: "service",
 *                 tagValue: "ec2",
 *             }],
 *         },
 *     ],
 * });
 * const exampleCostCustomForecast = new datadog.CostCustomForecast("example", {
 *     budgetUid: example.budgetId,
 *     entries: [{
 *         amount: 900,
 *         month: 202601,
 *         tagFilters: [{
 *             tagKey: "service",
 *             tagValue: "ec2",
 *         }],
 *     }],
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * Cost custom forecasts are imported using the parent budget's UUID, e.g.
 *
 * ```sh
 * $ pulumi import datadog:index/costCustomForecast:CostCustomForecast example a1b2c3d4-e5f6-7890-abcd-ef1234567890
 * ```
 */
export class CostCustomForecast extends pulumi.CustomResource {
    /**
     * Get an existing CostCustomForecast resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: CostCustomForecastState, opts?: pulumi.CustomResourceOptions): CostCustomForecast {
        return new CostCustomForecast(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'datadog:index/costCustomForecast:CostCustomForecast';

    /**
     * Returns true if the given object is an instance of CostCustomForecast.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is CostCustomForecast {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === CostCustomForecast.__pulumiType;
    }

    /**
     * The UUID of the budget that this custom forecast belongs to. Changing this value forces a new resource to be created.
     */
    declare public readonly budgetUid: pulumi.Output<string>;
    /**
     * Timestamp the custom forecast was created, in Unix milliseconds.
     */
    declare public /*out*/ readonly createdAt: pulumi.Output<number>;
    /**
     * The ID of the user that created the custom forecast.
     */
    declare public /*out*/ readonly createdBy: pulumi.Output<string>;
    /**
     * Monthly custom forecast entries. Each entry overrides the forecast for one `(month, tag_filters)` combination that must already exist as a budget entry. To remove all custom forecast entries, destroy this resource rather than setting an empty `entries` set.
     */
    declare public readonly entries: pulumi.Output<outputs.CostCustomForecastEntry[] | undefined>;
    /**
     * Timestamp the custom forecast was last updated, in Unix milliseconds.
     */
    declare public /*out*/ readonly updatedAt: pulumi.Output<number>;
    /**
     * The ID of the user that last updated the custom forecast.
     */
    declare public /*out*/ readonly updatedBy: pulumi.Output<string>;

    /**
     * Create a CostCustomForecast resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: CostCustomForecastArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: CostCustomForecastArgs | CostCustomForecastState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as CostCustomForecastState | undefined;
            resourceInputs["budgetUid"] = state?.budgetUid;
            resourceInputs["createdAt"] = state?.createdAt;
            resourceInputs["createdBy"] = state?.createdBy;
            resourceInputs["entries"] = state?.entries;
            resourceInputs["updatedAt"] = state?.updatedAt;
            resourceInputs["updatedBy"] = state?.updatedBy;
        } else {
            const args = argsOrState as CostCustomForecastArgs | undefined;
            if (args?.budgetUid === undefined && !opts.urn) {
                throw new Error("Missing required property 'budgetUid'");
            }
            resourceInputs["budgetUid"] = args?.budgetUid;
            resourceInputs["entries"] = args?.entries;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["createdBy"] = undefined /*out*/;
            resourceInputs["updatedAt"] = undefined /*out*/;
            resourceInputs["updatedBy"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(CostCustomForecast.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering CostCustomForecast resources.
 */
export interface CostCustomForecastState {
    /**
     * The UUID of the budget that this custom forecast belongs to. Changing this value forces a new resource to be created.
     */
    budgetUid?: pulumi.Input<string | undefined>;
    /**
     * Timestamp the custom forecast was created, in Unix milliseconds.
     */
    createdAt?: pulumi.Input<number | undefined>;
    /**
     * The ID of the user that created the custom forecast.
     */
    createdBy?: pulumi.Input<string | undefined>;
    /**
     * Monthly custom forecast entries. Each entry overrides the forecast for one `(month, tag_filters)` combination that must already exist as a budget entry. To remove all custom forecast entries, destroy this resource rather than setting an empty `entries` set.
     */
    entries?: pulumi.Input<pulumi.Input<inputs.CostCustomForecastEntry>[] | undefined>;
    /**
     * Timestamp the custom forecast was last updated, in Unix milliseconds.
     */
    updatedAt?: pulumi.Input<number | undefined>;
    /**
     * The ID of the user that last updated the custom forecast.
     */
    updatedBy?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a CostCustomForecast resource.
 */
export interface CostCustomForecastArgs {
    /**
     * The UUID of the budget that this custom forecast belongs to. Changing this value forces a new resource to be created.
     */
    budgetUid: pulumi.Input<string>;
    /**
     * Monthly custom forecast entries. Each entry overrides the forecast for one `(month, tag_filters)` combination that must already exist as a budget entry. To remove all custom forecast entries, destroy this resource rather than setting an empty `entries` set.
     */
    entries?: pulumi.Input<pulumi.Input<inputs.CostCustomForecastEntry>[] | undefined>;
}
