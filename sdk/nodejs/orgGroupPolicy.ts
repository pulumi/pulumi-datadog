// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Provides a Datadog Org Group Policy resource. This can be used to create and manage policies attached to an org group.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as datadog from "@pulumi/datadog";
 *
 * const prod = new datadog.OrgGroup("prod", {name: "Production Environments"});
 * // Disables widget copy-paste for every member org of the prod group.
 * // enforcement_tier = "OVERRIDE_ALLOWED" means member orgs can still override the value;
 * // use "GROUP_MANAGED" to make the value immutable for members.
 * const example = new datadog.OrgGroupPolicy("example", {
 *     orgGroupId: prod.id,
 *     policyName: "is_widget_copy_paste_enabled",
 *     content: JSON.stringify({
 *         org_config: false,
 *     }),
 *     enforcementTier: "OVERRIDE_ALLOWED",
 * });
 * ```
 *
 * ## Behavior notes
 *
 * ### Side effects on member orgs
 *
 * Creating or updating a policy with a non-`GROUP_MANAGED` tier (`OVERRIDE_ALLOWED` or `DELEGATE`) triggers propagation across every member org in the group. For each member, the server compares the org's current config value to the policy value:
 *
 * - **Match:** No action.
 * - **Mismatch:** The server auto-creates a `datadog.OrgGroupPolicyOverride` for that org, recording its existing value so the org is exempted from the new policy.
 *
 * Auto-created overrides show up in the `datadog.getOrgGroupPolicyOverrides` data source and can be adopted into Terraform via import. See the override resource documentation for the full lifecycle.
 *
 * ### Transitioning to GROUP_MANAGED
 *
 * Changing `enforcementTier` to `"GROUP_MANAGED"` automatically deletes every override associated with this policy server-side. Any `datadog.OrgGroupPolicyOverride` resources pointing at this policy must be removed from configuration in the same commit. Otherwise, Terraform's next apply will try to recreate the server-deleted overrides and fail with a `FailedPrecondition` error.
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import datadog:index/orgGroupPolicy:OrgGroupPolicy example <policy_uuid>
 * ```
 */
export class OrgGroupPolicy extends pulumi.CustomResource {
    /**
     * Get an existing OrgGroupPolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: OrgGroupPolicyState, opts?: pulumi.CustomResourceOptions): OrgGroupPolicy {
        return new OrgGroupPolicy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'datadog:index/orgGroupPolicy:OrgGroupPolicy';

    /**
     * Returns true if the given object is an instance of OrgGroupPolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is OrgGroupPolicy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === OrgGroupPolicy.__pulumiType;
    }

    /**
     * The policy content as a JSON-encoded string.
     */
    declare public readonly content: pulumi.Output<string>;
    /**
     * The enforcement tier of the policy. `OVERRIDE_ALLOWED` means the policy is set but member orgs may mutate it. `GROUP_MANAGED` means the policy is strictly controlled and mutations are blocked for affected orgs. `DELEGATE` means each member org controls its own value. Valid values are `OVERRIDE_ALLOWED`, `GROUP_MANAGED`, `DELEGATE`.
     */
    declare public readonly enforcementTier: pulumi.Output<string>;
    /**
     * The UUID of the org group this policy belongs to. Must be a valid UUID.
     */
    declare public readonly orgGroupId: pulumi.Output<string>;
    /**
     * The name of the policy. String length must be at least 1.
     */
    declare public readonly policyName: pulumi.Output<string>;
    /**
     * The type of the policy. Valid values are `orgConfig`.
     */
    declare public readonly policyType: pulumi.Output<string>;

    /**
     * Create a OrgGroupPolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: OrgGroupPolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: OrgGroupPolicyArgs | OrgGroupPolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as OrgGroupPolicyState | undefined;
            resourceInputs["content"] = state?.content;
            resourceInputs["enforcementTier"] = state?.enforcementTier;
            resourceInputs["orgGroupId"] = state?.orgGroupId;
            resourceInputs["policyName"] = state?.policyName;
            resourceInputs["policyType"] = state?.policyType;
        } else {
            const args = argsOrState as OrgGroupPolicyArgs | undefined;
            if (args?.content === undefined && !opts.urn) {
                throw new Error("Missing required property 'content'");
            }
            if (args?.orgGroupId === undefined && !opts.urn) {
                throw new Error("Missing required property 'orgGroupId'");
            }
            if (args?.policyName === undefined && !opts.urn) {
                throw new Error("Missing required property 'policyName'");
            }
            resourceInputs["content"] = args?.content;
            resourceInputs["enforcementTier"] = args?.enforcementTier;
            resourceInputs["orgGroupId"] = args?.orgGroupId;
            resourceInputs["policyName"] = args?.policyName;
            resourceInputs["policyType"] = args?.policyType;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(OrgGroupPolicy.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering OrgGroupPolicy resources.
 */
export interface OrgGroupPolicyState {
    /**
     * The policy content as a JSON-encoded string.
     */
    content?: pulumi.Input<string | undefined>;
    /**
     * The enforcement tier of the policy. `OVERRIDE_ALLOWED` means the policy is set but member orgs may mutate it. `GROUP_MANAGED` means the policy is strictly controlled and mutations are blocked for affected orgs. `DELEGATE` means each member org controls its own value. Valid values are `OVERRIDE_ALLOWED`, `GROUP_MANAGED`, `DELEGATE`.
     */
    enforcementTier?: pulumi.Input<string | undefined>;
    /**
     * The UUID of the org group this policy belongs to. Must be a valid UUID.
     */
    orgGroupId?: pulumi.Input<string | undefined>;
    /**
     * The name of the policy. String length must be at least 1.
     */
    policyName?: pulumi.Input<string | undefined>;
    /**
     * The type of the policy. Valid values are `orgConfig`.
     */
    policyType?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a OrgGroupPolicy resource.
 */
export interface OrgGroupPolicyArgs {
    /**
     * The policy content as a JSON-encoded string.
     */
    content: pulumi.Input<string>;
    /**
     * The enforcement tier of the policy. `OVERRIDE_ALLOWED` means the policy is set but member orgs may mutate it. `GROUP_MANAGED` means the policy is strictly controlled and mutations are blocked for affected orgs. `DELEGATE` means each member org controls its own value. Valid values are `OVERRIDE_ALLOWED`, `GROUP_MANAGED`, `DELEGATE`.
     */
    enforcementTier?: pulumi.Input<string | undefined>;
    /**
     * The UUID of the org group this policy belongs to. Must be a valid UUID.
     */
    orgGroupId: pulumi.Input<string>;
    /**
     * The name of the policy. String length must be at least 1.
     */
    policyName: pulumi.Input<string>;
    /**
     * The type of the policy. Valid values are `orgConfig`.
     */
    policyType?: pulumi.Input<string | undefined>;
}
