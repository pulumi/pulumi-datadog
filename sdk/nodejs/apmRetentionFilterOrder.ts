// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Provides a Datadog [APM Retention Filters API](https://docs.datadoghq.com/api/v2/apm-retention-filters/) resource, which is used to manage Datadog APM retention filters order.
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * Import existing APM retention filter order
 *
 * Note: Value of <foo> can be anything as this id is not stored by the resource
 *
 * ```sh
 * $ pulumi import datadog:index/apmRetentionFilterOrder:ApmRetentionFilterOrder bar <foo>
 * ```
 */
export class ApmRetentionFilterOrder extends pulumi.CustomResource {
    /**
     * Get an existing ApmRetentionFilterOrder resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ApmRetentionFilterOrderState, opts?: pulumi.CustomResourceOptions): ApmRetentionFilterOrder {
        return new ApmRetentionFilterOrder(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'datadog:index/apmRetentionFilterOrder:ApmRetentionFilterOrder';

    /**
     * Returns true if the given object is an instance of ApmRetentionFilterOrder.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ApmRetentionFilterOrder {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ApmRetentionFilterOrder.__pulumiType;
    }

    /**
     * The filter IDs list. The order of filters IDs in this attribute defines the overall APM retention filters order.
     */
    declare public readonly filterIds: pulumi.Output<string[]>;

    /**
     * Create a ApmRetentionFilterOrder resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ApmRetentionFilterOrderArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ApmRetentionFilterOrderArgs | ApmRetentionFilterOrderState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ApmRetentionFilterOrderState | undefined;
            resourceInputs["filterIds"] = state?.filterIds;
        } else {
            const args = argsOrState as ApmRetentionFilterOrderArgs | undefined;
            if (args?.filterIds === undefined && !opts.urn) {
                throw new Error("Missing required property 'filterIds'");
            }
            resourceInputs["filterIds"] = args?.filterIds;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ApmRetentionFilterOrder.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ApmRetentionFilterOrder resources.
 */
export interface ApmRetentionFilterOrderState {
    /**
     * The filter IDs list. The order of filters IDs in this attribute defines the overall APM retention filters order.
     */
    filterIds?: pulumi.Input<pulumi.Input<string>[]>;
}

/**
 * The set of arguments for constructing a ApmRetentionFilterOrder resource.
 */
export interface ApmRetentionFilterOrderArgs {
    /**
     * The filter IDs list. The order of filters IDs in this attribute defines the overall APM retention filters order.
     */
    filterIds: pulumi.Input<pulumi.Input<string>[]>;
}
