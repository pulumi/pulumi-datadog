// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Provides a Datadog Execution Policy resource. Execution policies control which Action Platform actions may run against your infrastructure, and where. Each policy pairs an effect (`allow` or `deny`) with a pattern of actions, optionally narrowed to specific Kubernetes namespaces, scripts or remote shell paths, and optionally scoped to agents matching a set of Fleet Automation tags.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as datadog from "@pulumi/datadog";
 *
 * // Kubernetes: allow read-only actions against prod namespaces, on agents/PARs
 * // matching either target tag set below.
 * const kubernetesProdRead = new datadog.ActionExecutionPolicy("kubernetes_prod_read", {
 *     name: "allow-kubernetes-prod-read",
 *     effect: "allow",
 *     actionPattern: [{
 *         integration: "INTEGRATION_KUBERNETES",
 *         actionFqns: [
 *             "com.datadoghq.kubernetes.core.getPod",
 *             "com.datadoghq.kubernetes.core.listPod",
 *         ],
 *     }],
 *     scope: [{
 *         kubernetes: [{
 *             rules: [{
 *                 targetNamespaces: [
 *                     "prod",
 *                     "prod-system",
 *                 ],
 *             }],
 *         }],
 *     }],
 *     targets: [
 *         {
 *             name: "prod-us",
 *             agentTags: [
 *                 "env:prod",
 *                 "region:us-east-1",
 *             ],
 *         },
 *         {
 *             name: "prod-eu",
 *             agentTags: [
 *                 "env:prod",
 *                 "region:eu-west-1",
 *             ],
 *         },
 *     ],
 * });
 * // Remote action (rshell): allow command execution restricted to a read-only path on staging agents.
 * const rshellStagingReadonly = new datadog.ActionExecutionPolicy("rshell_staging_readonly", {
 *     name: "rshell-staging-readonly",
 *     effect: "allow",
 *     actionPattern: [{
 *         integration: "INTEGRATION_REMOTE_ACTION",
 *         actionFqns: ["com.datadoghq.remoteaction.rshell.runCommand"],
 *     }],
 *     scope: [{
 *         remoteActionRshell: [{
 *             rules: [{
 *                 targetPaths: ["/etc/datadog-agent/"],
 *                 access: "read_only",
 *             }],
 *         }],
 *     }],
 *     targets: [{
 *         name: "staging",
 *         agentTags: ["env:staging"],
 *     }],
 * });
 * // Scripts: deny a specific shell script action everywhere, regardless of agent tags
 * // (no `target` block means the policy applies fleet-wide).
 * const denyDangerousScript = new datadog.ActionExecutionPolicy("deny_dangerous_script", {
 *     name: "deny-dangerous-cleanup-script",
 *     effect: "deny",
 *     actionPattern: [{
 *         integration: "INTEGRATION_SCRIPT",
 *         actionFqns: ["com.datadoghq.script.runShellScript"],
 *     }],
 *     scope: [{
 *         scripts: [{
 *             rules: [{
 *                 targetScriptNames: ["dangerous-cleanup.sh"],
 *             }],
 *         }],
 *     }],
 * });
 * // Access to a policy is managed separately via datadog_restriction_policy,
 * // keyed by "execution-policy:<id>".
 * const kubernetesProdPerms = new datadog.RestrictionPolicy("kubernetes_prod_perms", {
 *     resourceId: pulumi.interpolate`execution-policy:${kubernetesProdRead.id}`,
 *     bindings: [{
 *         relation: "editor",
 *         principals: ["role:sre-oncall"],
 *     }],
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * ```sh
 * $ pulumi import datadog:index/actionExecutionPolicy:ActionExecutionPolicy my_policy 11111111-2222-3333-4444-555555555555
 * ```
 */
export class ActionExecutionPolicy extends pulumi.CustomResource {
    /**
     * Get an existing ActionExecutionPolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ActionExecutionPolicyState, opts?: pulumi.CustomResourceOptions): ActionExecutionPolicy {
        return new ActionExecutionPolicy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'datadog:index/actionExecutionPolicy:ActionExecutionPolicy';

    /**
     * Returns true if the given object is an instance of ActionExecutionPolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ActionExecutionPolicy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ActionExecutionPolicy.__pulumiType;
    }

    /**
     * The set of actions this policy applies to. Required.
     */
    declare public readonly actionPattern: pulumi.Output<outputs.ActionExecutionPolicyActionPattern | undefined>;
    /**
     * The date and time the execution policy was created, as an RFC3339 timestamp.
     */
    declare public /*out*/ readonly createdAt: pulumi.Output<string>;
    /**
     * The ID of the user who created the execution policy.
     */
    declare public /*out*/ readonly createdBy: pulumi.Output<string>;
    /**
     * Whether the policy allows or denies the matched actions. Valid values are `allow`, `deny`.
     */
    declare public readonly effect: pulumi.Output<string>;
    /**
     * The name of the execution policy.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Restricts where the policy applies, beyond `actionPattern`. When configured, exactly one of `kubernetes`, `scripts` or `remoteActionRshell` must be set, and it must match `action_pattern.integration`. Omitting this block means the policy has no scope restriction.
     */
    declare public readonly scope: pulumi.Output<outputs.ActionExecutionPolicyScope | undefined>;
    /**
     * A target this policy is scoped to, expressed as a set of Agent tags. Each target is matched independently; omitting all `target` blocks applies the policy fleet-wide.
     */
    declare public readonly targets: pulumi.Output<outputs.ActionExecutionPolicyTarget[] | undefined>;
    /**
     * The date and time the execution policy was last updated, as an RFC3339 timestamp.
     */
    declare public /*out*/ readonly updatedAt: pulumi.Output<string>;
    /**
     * The ID of the user who last updated the execution policy.
     */
    declare public /*out*/ readonly updatedBy: pulumi.Output<string>;
    /**
     * The version of the execution policy. Incremented by Datadog on every update.
     */
    declare public /*out*/ readonly version: pulumi.Output<number>;

    /**
     * Create a ActionExecutionPolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ActionExecutionPolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ActionExecutionPolicyArgs | ActionExecutionPolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ActionExecutionPolicyState | undefined;
            resourceInputs["actionPattern"] = state?.actionPattern;
            resourceInputs["createdAt"] = state?.createdAt;
            resourceInputs["createdBy"] = state?.createdBy;
            resourceInputs["effect"] = state?.effect;
            resourceInputs["name"] = state?.name;
            resourceInputs["scope"] = state?.scope;
            resourceInputs["targets"] = state?.targets;
            resourceInputs["updatedAt"] = state?.updatedAt;
            resourceInputs["updatedBy"] = state?.updatedBy;
            resourceInputs["version"] = state?.version;
        } else {
            const args = argsOrState as ActionExecutionPolicyArgs | undefined;
            if (args?.effect === undefined && !opts.urn) {
                throw new Error("Missing required property 'effect'");
            }
            if (args?.name === undefined && !opts.urn) {
                throw new Error("Missing required property 'name'");
            }
            resourceInputs["actionPattern"] = args?.actionPattern;
            resourceInputs["effect"] = args?.effect;
            resourceInputs["name"] = args?.name;
            resourceInputs["scope"] = args?.scope;
            resourceInputs["targets"] = args?.targets;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["createdBy"] = undefined /*out*/;
            resourceInputs["updatedAt"] = undefined /*out*/;
            resourceInputs["updatedBy"] = undefined /*out*/;
            resourceInputs["version"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ActionExecutionPolicy.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ActionExecutionPolicy resources.
 */
export interface ActionExecutionPolicyState {
    /**
     * The set of actions this policy applies to. Required.
     */
    actionPattern?: pulumi.Input<inputs.ActionExecutionPolicyActionPattern | undefined>;
    /**
     * The date and time the execution policy was created, as an RFC3339 timestamp.
     */
    createdAt?: pulumi.Input<string | undefined>;
    /**
     * The ID of the user who created the execution policy.
     */
    createdBy?: pulumi.Input<string | undefined>;
    /**
     * Whether the policy allows or denies the matched actions. Valid values are `allow`, `deny`.
     */
    effect?: pulumi.Input<string | undefined>;
    /**
     * The name of the execution policy.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Restricts where the policy applies, beyond `actionPattern`. When configured, exactly one of `kubernetes`, `scripts` or `remoteActionRshell` must be set, and it must match `action_pattern.integration`. Omitting this block means the policy has no scope restriction.
     */
    scope?: pulumi.Input<inputs.ActionExecutionPolicyScope | undefined>;
    /**
     * A target this policy is scoped to, expressed as a set of Agent tags. Each target is matched independently; omitting all `target` blocks applies the policy fleet-wide.
     */
    targets?: pulumi.Input<pulumi.Input<inputs.ActionExecutionPolicyTarget>[] | undefined>;
    /**
     * The date and time the execution policy was last updated, as an RFC3339 timestamp.
     */
    updatedAt?: pulumi.Input<string | undefined>;
    /**
     * The ID of the user who last updated the execution policy.
     */
    updatedBy?: pulumi.Input<string | undefined>;
    /**
     * The version of the execution policy. Incremented by Datadog on every update.
     */
    version?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a ActionExecutionPolicy resource.
 */
export interface ActionExecutionPolicyArgs {
    /**
     * The set of actions this policy applies to. Required.
     */
    actionPattern?: pulumi.Input<inputs.ActionExecutionPolicyActionPattern | undefined>;
    /**
     * Whether the policy allows or denies the matched actions. Valid values are `allow`, `deny`.
     */
    effect: pulumi.Input<string>;
    /**
     * The name of the execution policy.
     */
    name: pulumi.Input<string>;
    /**
     * Restricts where the policy applies, beyond `actionPattern`. When configured, exactly one of `kubernetes`, `scripts` or `remoteActionRshell` must be set, and it must match `action_pattern.integration`. Omitting this block means the policy has no scope restriction.
     */
    scope?: pulumi.Input<inputs.ActionExecutionPolicyScope | undefined>;
    /**
     * A target this policy is scoped to, expressed as a set of Agent tags. Each target is matched independently; omitting all `target` blocks applies the policy fleet-wide.
     */
    targets?: pulumi.Input<pulumi.Input<inputs.ActionExecutionPolicyTarget>[] | undefined>;
}
