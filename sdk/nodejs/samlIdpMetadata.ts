// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Provides a Datadog SAML IdP metadata resource. This can be used to upload or replace the identity provider (IdP) metadata used for the organization's SAML login configuration. An organization has at most one SAML configuration. Note: the uploaded metadata XML cannot be read back from the Datadog API, so changes made outside of Terraform to the metadata content are not detected. Destroying this resource removes it from Terraform state but does not remove the IdP metadata from Datadog.
 */
export class SamlIdpMetadata extends pulumi.CustomResource {
    /**
     * Get an existing SamlIdpMetadata resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SamlIdpMetadataState, opts?: pulumi.CustomResourceOptions): SamlIdpMetadata {
        return new SamlIdpMetadata(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'datadog:index/samlIdpMetadata:SamlIdpMetadata';

    /**
     * Returns true if the given object is an instance of SamlIdpMetadata.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SamlIdpMetadata {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SamlIdpMetadata.__pulumiType;
    }

    /**
     * The assertion consumer service (ACS) URLs that the identity provider posts SAML responses to.
     */
    declare public /*out*/ readonly assertionConsumerServices: pulumi.Output<string[]>;
    /**
     * The service provider entity ID Datadog presents to the identity provider.
     */
    declare public /*out*/ readonly entityId: pulumi.Output<string>;
    /**
     * The timestamp (RFC3339) at which the uploaded identity provider metadata expires.
     */
    declare public /*out*/ readonly expiresAt: pulumi.Output<string>;
    /**
     * The content of the IdP metadata XML file, for example loaded with the `file()` function. A leading UTF-8 byte order mark (BOM) is stripped automatically before upload.
     */
    declare public readonly idpMetadata: pulumi.Output<string>;
    /**
     * The single sign-on URL users can visit to start a SAML login. `null` when the organization is identity-provider-initiated and has no subdomain.
     */
    declare public /*out*/ readonly ssoUrl: pulumi.Output<string>;

    /**
     * Create a SamlIdpMetadata resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SamlIdpMetadataArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SamlIdpMetadataArgs | SamlIdpMetadataState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SamlIdpMetadataState | undefined;
            resourceInputs["assertionConsumerServices"] = state?.assertionConsumerServices;
            resourceInputs["entityId"] = state?.entityId;
            resourceInputs["expiresAt"] = state?.expiresAt;
            resourceInputs["idpMetadata"] = state?.idpMetadata;
            resourceInputs["ssoUrl"] = state?.ssoUrl;
        } else {
            const args = argsOrState as SamlIdpMetadataArgs | undefined;
            if (args?.idpMetadata === undefined && !opts.urn) {
                throw new Error("Missing required property 'idpMetadata'");
            }
            resourceInputs["idpMetadata"] = args?.idpMetadata;
            resourceInputs["assertionConsumerServices"] = undefined /*out*/;
            resourceInputs["entityId"] = undefined /*out*/;
            resourceInputs["expiresAt"] = undefined /*out*/;
            resourceInputs["ssoUrl"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SamlIdpMetadata.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SamlIdpMetadata resources.
 */
export interface SamlIdpMetadataState {
    /**
     * The assertion consumer service (ACS) URLs that the identity provider posts SAML responses to.
     */
    assertionConsumerServices?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The service provider entity ID Datadog presents to the identity provider.
     */
    entityId?: pulumi.Input<string | undefined>;
    /**
     * The timestamp (RFC3339) at which the uploaded identity provider metadata expires.
     */
    expiresAt?: pulumi.Input<string | undefined>;
    /**
     * The content of the IdP metadata XML file, for example loaded with the `file()` function. A leading UTF-8 byte order mark (BOM) is stripped automatically before upload.
     */
    idpMetadata?: pulumi.Input<string | undefined>;
    /**
     * The single sign-on URL users can visit to start a SAML login. `null` when the organization is identity-provider-initiated and has no subdomain.
     */
    ssoUrl?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a SamlIdpMetadata resource.
 */
export interface SamlIdpMetadataArgs {
    /**
     * The content of the IdP metadata XML file, for example loaded with the `file()` function. A leading UTF-8 byte order mark (BOM) is stripped automatically before upload.
     */
    idpMetadata: pulumi.Input<string>;
}
