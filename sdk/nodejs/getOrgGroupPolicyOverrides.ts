// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Use this data source to retrieve org group policy overrides. Supports filtering by policy ID (server-side) and organization UUID (client-side).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as datadog from "@pulumi/datadog";
 *
 * // List every override attached to the given org group. Useful for discovering
 * // overrides auto-created by the server in response to membership or policy events.
 * const all = datadog.getOrgGroupPolicyOverrides({
 *     orgGroupId: "019d97e8-74d4-7060-806e-e731c8f1d005",
 * });
 * // Scope the search to a single policy and/or organization. org_uuid is a
 * // client-side filter since the API does not natively support it.
 * const forOrg = datadog.getOrgGroupPolicyOverrides({
 *     orgGroupId: "019d97e8-74d4-7060-806e-e731c8f1d005",
 *     policyId: "019d97e9-f340-78c1-aa50-6a953c2bd006",
 *     orgUuid: "ff4a8255-6931-58d1-add0-a6b3602d5421",
 * });
 * ```
 */
export function getOrgGroupPolicyOverrides(args: GetOrgGroupPolicyOverridesArgs, opts?: pulumi.InvokeOptions): Promise<GetOrgGroupPolicyOverridesResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("datadog:index/getOrgGroupPolicyOverrides:getOrgGroupPolicyOverrides", {
        "orgGroupId": args.orgGroupId,
        "orgUuid": args.orgUuid,
        "policyId": args.policyId,
    }, opts);
}

/**
 * A collection of arguments for invoking getOrgGroupPolicyOverrides.
 */
export interface GetOrgGroupPolicyOverridesArgs {
    /**
     * The UUID of the org group whose overrides to list. Must be a valid UUID.
     */
    orgGroupId: string;
    /**
     * Filter overrides to those for the given organization. Applied client-side after the List call because the API does not accept an `orgUuid` filter on this endpoint. Must be a valid UUID.
     */
    orgUuid?: string;
    /**
     * Filter overrides to those on the given policy. Must be a valid UUID.
     */
    policyId?: string;
}

/**
 * A collection of values returned by getOrgGroupPolicyOverrides.
 */
export interface GetOrgGroupPolicyOverridesResult {
    /**
     * The ID of this resource.
     */
    readonly id: string;
    /**
     * The UUID of the org group whose overrides to list. Must be a valid UUID.
     */
    readonly orgGroupId: string;
    /**
     * Filter overrides to those for the given organization. Applied client-side after the List call because the API does not accept an `orgUuid` filter on this endpoint. Must be a valid UUID.
     */
    readonly orgUuid?: string;
    /**
     * The list of policy overrides.
     */
    readonly overrides: outputs.GetOrgGroupPolicyOverridesOverride[];
    /**
     * Filter overrides to those on the given policy. Must be a valid UUID.
     */
    readonly policyId?: string;
}
/**
 * Use this data source to retrieve org group policy overrides. Supports filtering by policy ID (server-side) and organization UUID (client-side).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as datadog from "@pulumi/datadog";
 *
 * // List every override attached to the given org group. Useful for discovering
 * // overrides auto-created by the server in response to membership or policy events.
 * const all = datadog.getOrgGroupPolicyOverrides({
 *     orgGroupId: "019d97e8-74d4-7060-806e-e731c8f1d005",
 * });
 * // Scope the search to a single policy and/or organization. org_uuid is a
 * // client-side filter since the API does not natively support it.
 * const forOrg = datadog.getOrgGroupPolicyOverrides({
 *     orgGroupId: "019d97e8-74d4-7060-806e-e731c8f1d005",
 *     policyId: "019d97e9-f340-78c1-aa50-6a953c2bd006",
 *     orgUuid: "ff4a8255-6931-58d1-add0-a6b3602d5421",
 * });
 * ```
 */
export function getOrgGroupPolicyOverridesOutput(args: GetOrgGroupPolicyOverridesOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetOrgGroupPolicyOverridesResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("datadog:index/getOrgGroupPolicyOverrides:getOrgGroupPolicyOverrides", {
        "orgGroupId": args.orgGroupId,
        "orgUuid": args.orgUuid,
        "policyId": args.policyId,
    }, opts);
}

/**
 * A collection of arguments for invoking getOrgGroupPolicyOverrides.
 */
export interface GetOrgGroupPolicyOverridesOutputArgs {
    /**
     * The UUID of the org group whose overrides to list. Must be a valid UUID.
     */
    orgGroupId: pulumi.Input<string>;
    /**
     * Filter overrides to those for the given organization. Applied client-side after the List call because the API does not accept an `orgUuid` filter on this endpoint. Must be a valid UUID.
     */
    orgUuid?: pulumi.Input<string | undefined>;
    /**
     * Filter overrides to those on the given policy. Must be a valid UUID.
     */
    policyId?: pulumi.Input<string | undefined>;
}
